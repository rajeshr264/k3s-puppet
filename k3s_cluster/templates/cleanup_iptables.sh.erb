#!/bin/bash
# K3S iptables cleanup script
# Generated by Puppet - do not edit manually

# Remove K3S-specific iptables rules
iptables -t nat -F || true
iptables -t mangle -F || true
iptables -F || true
iptables -X || true

# Remove K3S chains
iptables -t nat -X KUBE-SERVICES || true
iptables -t nat -X KUBE-NODEPORTS || true
iptables -t nat -X KUBE-POSTROUTING || true
iptables -t nat -X KUBE-MARK-MASQ || true

echo "K3S iptables rules cleaned up" 